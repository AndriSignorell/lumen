% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpqr-revgumbel.R
\name{dpqr-revgumbel}
\alias{dpqr-revgumbel}
\alias{drevgumbel}
\alias{prevgumbel}
\alias{qrevgumbel}
\alias{qrevgumbelExp}
\alias{rrevgumbel}
\title{Reverse Gumbel Distribution}
\usage{
drevgumbel(x, loc = 0, scale = 1, log = FALSE)

prevgumbel(q, loc = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)

qrevgumbel(p, loc = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)

rrevgumbel(n, loc = 0, scale = 1)

qrevgumbelExp(p, loc = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)
}
\arguments{
\item{x, q}{numeric vector of abscissa (or quantile) values at which to
evaluate the density or distribution function.}

\item{loc}{location of the distribution.}

\item{scale}{scale (\eqn{> 0}) of the distribution.}

\item{log, log.p}{logical; if \code{TRUE}, probabilities \code{p} are given as
\code{log(p)} and the density is returned on the log scale.}

\item{lower.tail}{logical; if \code{TRUE} (default), probabilities are
\verb{P[X <= x]}, otherwise, P\verb{[X > x]}.}

\item{p}{numeric vector of probabilities at which to evaluate the quantile
function.}

\item{n}{number of random variates, i.e., \code{\link[=length]{length()}} of resulting
vector of \code{rrevgumbel()}.}
}
\value{
A numeric vector, of the same length as \code{x}, \code{q}, or
\code{p} for the first three functions, and of length \code{n} for
\code{rrevgumbel()}. \code{qrevgumbelExp()} gives the quantiles of
\eqn{\exp(X)}, the exponential parametrization used in some applications.
}
\description{
Density, distribution function, quantile function and random generation for
the \dQuote{Reverse} Gumbel distribution with parameters \code{loc} and
\code{scale}.
}
\details{
The reverse Gumbel distribution is the distribution of \eqn{a - bY} for a
standard Gumbel \eqn{Y}, i.e. the Type I extreme value distribution for
minima. With \eqn{`loc` = a} and \eqn{`scale` = b} its distribution
function is
\deqn{F(x) = 1 - \exp\left\{-\exp\left[\left(\frac{x-a}{b}\right)\right]\right\}}{F(x) = 1 - exp(-exp((x-a)/b))}
for all real \eqn{x}, where \eqn{b > 0}.
}
\note{
Based on code by Werner Stahel, partly inspired by the \pkg{VGAM} package
(numeric refinements by Martin Maechler), adapted to conform to package
standards.
}
\examples{

curve(prevgumbel(x, scale= 1/2), -3,2, n=1001, col=1, lwd=2,
      main = "revgumbel(x, scale = 1/2)")
abline(h=0:1, v = 0, lty=3, col = "gray30")
curve(drevgumbel(x, scale= 1/2),       n=1001, add=TRUE,
      col = (col.d <- adjustcolor(2, 0.5)), lwd=3)
legend("left", c("cdf","pdf"), col=c("black", col.d), lwd=2:3, bty="n")

med <- qrevgumbel(0.5, scale=1/2)
cat("The median is:",  format(med),"\n")

}
\seealso{
\link{distributions-overview}; \link{dpqr-gumbel} for the Gumbel
distribution this one reverses.
}
\concept{distribution-function}
\concept{extreme-value}
