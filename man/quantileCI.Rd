% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantileCI.R
\name{quantileCI}
\alias{quantileCI}
\title{Confidence Interval for Any Quantile}
\usage{
quantileCI(
  x,
  conf.level = 0.95,
  sides = c("two.sided", "left", "right"),
  method = c("exact", "boot"),
  probs = seq(0, 1, 0.25),
  na.rm = FALSE,
  ...
)
}
\arguments{
\item{x}{a (non-empty) numeric vector of data values.}

\item{conf.level}{confidence level of the interval}

\item{sides}{a character string specifying the side of the confidence
interval, must be one of \code{"two.sided"} (default), \code{"left"} or
\code{"right"} (abbreviations allowed). \cr\code{"left"} would be analogue
to a \code{"greater"} hypothesis in a \code{t.test}.}

\item{method}{defining the type of interval that should be calculated (one
out of \code{"exact"}, \code{"boot"}). Default is \code{"exact"}. See
Details.}

\item{probs}{numeric vector of probabilities with values in \emph{\verb{[0,1]}}.
(Values up to \code{2e-14} outside that range are accepted and moved to the
nearby endpoint.)}

\item{na.rm}{logical. Should missing values be removed? Defaults to
\code{FALSE}.}

\item{\dots}{bootstrap arguments can be provided by the dots argument. See
\code{\link[boot]{boot.ci}} for details.}
}
\value{
if probs was of length 1 a numeric vector with 3 elements:
\item{est}{estimator} \item{lci}{lower bound of the confidence interval}
\item{uci}{upper bound of the confidence interval}

or, if probs was a vector, a matrix with 3 columns consisting of estimate,
lower ci, upper ci \code{est, lci, uci}
}
\description{
Calculates the confidence interval for any quantile. Although bootstrapping
might be a good approach for getting senisble confidence intervals there's
sometimes need to have a nonparameteric alternative. This function offers
one.
}
\details{
The \code{"exact"} method corresponds to the way the confidence interval for
the median is calculated in SAS. \cr The boot confidence interval type is
calculated by means of \code{\link[boot]{boot.ci}} with default type
\code{"basic"}.
}
\examples{

x <- mtcars$mpg
quantileCI(x, probs=0.25, na.rm=TRUE)

quantileCI(x, na.rm=TRUE)
quantileCI(x, conf.level=0.99, na.rm=TRUE)

# multiple probs
quantileCI(1:100, method="exact" , probs = c(0.25, 0.75, .80, 0.95))
quantileCI(1:100, method="boot" , probs = c(0.25, 0.75, .80, 0.95))


}
\seealso{
\code{\link[DescToolsX]{quantileX}}, \code{\link{quantile}},
\code{\link{medianCI}}
}
\author{
Andri Signorell \href{mailto:andri@signorell.net}{andri@signorell.net} based on code of W Huber on
StackExchange
}
